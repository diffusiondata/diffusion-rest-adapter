
(function() {
    // TODO: Bootstrap TypScript without JavaScript
    // Pull in Angular 2 dependencies
    var reflect = require('reflect-metadata');
    var zone = require('zone.js');
    // Pull in Angular 2 application
    var main = require('./main');
})();
